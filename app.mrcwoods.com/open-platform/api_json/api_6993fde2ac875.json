{
    "name": "图片内容描述",
    "brief": "快速掌握图片中的关键信息",
    "request_desc": "前往https://yunzhiapi.cn/ 登录获取用户密钥token",
    "api_url": "https://yunzhiapi.cn/API/tpnrms.php",
    "request_method": "GET",
    "return_format": "TEXT",
    "request_example": "https://yunzhiapi.cn/API/tpnrms.php?url=图片链接",
    "request_params": [
        {
            "name": "token",
            "type": "string",
            "required": "是",
            "desc": "登录获取用户密钥"
        },
        {
            "name": "url",
            "type": "string",
            "required": "是",
            "desc": "需要识别的图片链接。推荐jpg、png、webp等标准图片格式，其他图链请配合本站的图片转Base64编码接口使用，并通过post请求传入(确保你的链接支持跨域并且无需鉴权，最大支持20MB)"
        },
        {
            "name": "type",
            "type": "string",
            "required": "可选",
            "desc": "返回数据格式，支持json和text，不填写默认返回text"
        }
    ],
    "return_params": [
        {
            "field": "image_url",
            "type": "url",
            "desc": "识别图片"
        },
        {
            "field": "description",
            "type": "string",
            "desc": "图片描述"
        }
    ],
    "status_code": [
        {
            "code": "201",
            "desc": "请求失败"
        },
        {
            "code": "400",
            "desc": "参数错误"
        },
        {
            "code": "429",
            "desc": "请求超限"
        }
    ],
    "return_example": "这是一张色彩明亮、充满教育气息的宣传海报，整体采用卡通风格设计，旨在推广德语专四考试真题资料。画面的视觉中心是大段白色、带有立体阴影效果的中文文字，内容为“自动发货 德语专四真题 092到23年 都有最新合集 备考必备”，字体粗大醒目，清晰传达了产品信息和核心卖点。背景以浅蓝色和淡黄色为主色调，营造出一种清新、积极的氛围，上方是浅蓝色的天空，点缀着几朵白云和闪烁的星星，下方则有更浓密的云层和一道金色的光弧，增强了画面的层次感和梦幻感。在文字区域的上方，一条弯曲的胶片卷轴状图案贯穿画面，上面印有日历样式的日期网格，暗示了资料的时间跨度。胶片卷轴上还分布着几个与学习相关的图标：左侧是一个卷轴状的纸张，上面印有德国国旗（黑红金三色）；旁边是一支蓝色的钢笔；右侧则是一个黑色的学士帽、一个放大镜以及一个日历。这些元素共同强化了学习、考试和德国语言的主题。整体构图均衡，色彩搭配和谐，通过卡通化的视觉语言和明确的文字信息，有效地传达了产品信息，并激发目标受众（准备德语专四考试的学生）的兴趣与需求。",
    "sdk": {
        "JavaScript": "// 图片内容描述API的JavaScript实现\r\n\r\n// 必填参数\r\nconst token = &apos;YOUR_TOKEN_HERE&apos;; // 替换为你的用户密钥\r\nconst url = &apos;https://example.com/image.jpg&apos;; // 替换为需要识别的图片链接\r\n\r\n// 可选参数\r\n// const type = &apos;json&apos;; // 返回数据格式，支持json和text，不填写默认返回text\r\n\r\n// 请求URL\r\nconst requestUrl = &apos;https://yunzhiapi.cn/API/tpnrms.php&apos;;\r\n\r\n// 构建请求参数\r\nconst params = new URLSearchParams({\r\n    token: token,\r\n    url: url,\r\n    // type: type, // 如果需要指定返回数据格式，取消注释此行\r\n});\r\n\r\n// 发送请求\r\nfetch(requestUrl, {\r\n    method: &apos;POST&apos;, // 根据需要选择GET或POST\r\n    headers: {\r\n        &apos;Content-Type&apos;: &apos;application/x-www-form-urlencoded&apos;,\r\n    },\r\n    body: params.toString(),\r\n})\r\n.then(response =&gt; {\r\n    if (!response.ok) {\r\n        throw new Error(`HTTP error! Status: ${response.status}`);\r\n    }\r\n    // 根据返回数据格式处理响应\r\n    // if (type === &apos;json&apos;) {\r\n    //     return response.json();\r\n    // } else {\r\n        return response.text();\r\n    // }\r\n})\r\n.then(data =&gt; {\r\n    console.log(&apos;图片内容描述结果:&apos;, data);\r\n})\r\n.catch(error =&gt; {\r\n    console.error(&apos;请求失败:&apos;, error);\r\n});",
        "PHP": "&lt;?php\r\n\r\n/**\r\n * 图片内容描述API调用示例\r\n * 该API用于快速掌握图片中的关键信息\r\n */\r\n\r\n// 必填参数\r\n$token = &apos;YOUR_TOKEN_HERE&apos;; // 登录获取用户密钥\r\n$url = &apos;YOUR_IMAGE_URL_HERE&apos;; // 需要识别的图片链接\r\n\r\n// 可选参数，根据需要取消注释并设置\r\n// $type = &apos;json&apos;; // 返回数据格式，支持json和text，不填写默认返回text\r\n\r\n// 请求URL\r\n$requestUrl = &apos;https://yunzhiapi.cn/API/tpnrms.php&apos;;\r\n\r\n// 初始化cURL会话\r\n$ch = curl_init();\r\n\r\n// 设置cURL选项\r\ncurl_setopt($ch, CURLOPT_URL, $requestUrl);\r\ncurl_setopt($ch, CURLOPT_RETURNTRANSFER, true);\r\ncurl_setopt($ch, CURLOPT_POST, true);\r\n\r\n// 构建POST数据\r\n$postData = [\r\n    &apos;token&apos; =&gt; $token,\r\n    &apos;url&apos; =&gt; $url,\r\n    // &apos;type&apos; =&gt; $type, // 可选参数，根据需要取消注释\r\n];\r\n\r\ncurl_setopt($ch, CURLOPT_POSTFIELDS, http_build_query($postData));\r\n\r\n// 执行cURL请求\r\n$response = curl_exec($ch);\r\n\r\n// 检查cURL错误\r\nif (curl_errno($ch)) {\r\n    echo &apos;cURL error: &apos; . curl_error($ch);\r\n} else {\r\n    // 获取HTTP状态码\r\n    $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);\r\n\r\n    if ($httpCode == 200) {\r\n        // 请求成功，处理响应数据\r\n        echo &apos;Response: &apos; . $response;\r\n    } else {\r\n        // 请求失败，输出错误信息\r\n        echo &apos;Request failed with HTTP code: &apos; . $httpCode;\r\n    }\r\n}\r\n\r\n// 关闭cURL会话\r\ncurl_close($ch);\r\n?&gt;",
        "Java": "import java.io.BufferedReader;\r\nimport java.io.InputStreamReader;\r\nimport java.net.HttpURLConnection;\r\nimport java.net.URL;\r\nimport java.net.URLEncoder;\r\n\r\npublic class ImageDescriptionApi {\r\n\r\n    // 必填参数\r\n    private static final String TOKEN = &quot;your_token_here&quot;; // 登录获取用户密钥\r\n    private static final String URL = &quot;https://example.com/image.jpg&quot;; // 需要识别的图片链接\r\n\r\n    // 可选参数\r\n    // private static final String TYPE = &quot;json&quot;; // 返回数据格式，支持json和text，不填写默认返回text\r\n\r\n    public static void main(String[] args) {\r\n        try {\r\n            // 构建请求URL\r\n            StringBuilder requestUrl = new StringBuilder(&quot;https://yunzhiapi.cn/API/tpnrms.php?&quot;);\r\n            requestUrl.append(&quot;token=&quot;).append(URLEncoder.encode(TOKEN, &quot;UTF-8&quot;));\r\n            requestUrl.append(&quot;&amp;url=&quot;).append(URLEncoder.encode(URL, &quot;UTF-8&quot;));\r\n\r\n            // 如果需要使用可选参数type，取消下面一行的注释\r\n            // requestUrl.append(&quot;&amp;type=&quot;).append(URLEncoder.encode(TYPE, &quot;UTF-8&quot;));\r\n\r\n            // 创建URL对象\r\n            URL url = new URL(requestUrl.toString());\r\n            HttpURLConnection connection = (HttpURLConnection) url.openConnection();\r\n            connection.setRequestMethod(&quot;GET&quot;);\r\n\r\n            // 设置请求头\r\n            connection.setRequestProperty(&quot;Accept&quot;, &quot;application/json&quot;);\r\n\r\n            // 获取响应码\r\n            int responseCode = connection.getResponseCode();\r\n            if (responseCode == HttpURLConnection.HTTP_OK) {\r\n                // 读取响应内容\r\n                BufferedReader in = new BufferedReader(new InputStreamReader(connection.getInputStream()));\r\n                String inputLine;\r\n                StringBuilder response = new StringBuilder();\r\n                while ((inputLine = in.readLine()) != null) {\r\n                    response.append(inputLine);\r\n                }\r\n                in.close();\r\n\r\n                // 输出响应内容\r\n                System.out.println(&quot;Response: &quot; + response.toString());\r\n            } else {\r\n                System.err.println(&quot;Error: HTTP request failed with response code &quot; + responseCode);\r\n            }\r\n        } catch (Exception e) {\r\n            System.err.println(&quot;An error occurred: &quot; + e.getMessage());\r\n            e.printStackTrace();\r\n        }\r\n    }\r\n}",
        "Python": "import requests\r\n\r\ndef describe_image_content(token, url):\r\n    &quot;&quot;&quot;\r\n    调用图片内容描述API，获取图片中的关键信息。\r\n\r\n    :param token: 登录获取的用户密钥\r\n    :param url: 需要识别的图片链接\r\n    :return: API返回的结果\r\n    &quot;&quot;&quot;\r\n    # 必填参数\r\n    params = {\r\n        &apos;token&apos;: token,\r\n        &apos;url&apos;: url\r\n    }\r\n    \r\n    # 可选参数，根据需要取消注释\r\n    # params[&apos;type&apos;] = &apos;json&apos;  # 返回数据格式，支持json和text，默认返回text\r\n\r\n    # 请求URL\r\n    request_url = &quot;https://yunzhiapi.cn/API/tpnrms.php&quot;\r\n    \r\n    try:\r\n        # 发送GET请求\r\n        response = requests.get(request_url, params=params)\r\n        response.raise_for_status()  # 检查请求是否成功\r\n\r\n        # 根据返回格式解析数据\r\n        if params.get(&apos;type&apos;) == &apos;json&apos;:\r\n            return response.json()\r\n        else:\r\n            return response.text\r\n\r\n    except requests.exceptions.HTTPError as http_err:\r\n        print(f&quot;HTTP error occurred: {http_err}&quot;)  # HTTP请求错误\r\n    except requests.exceptions.ConnectionError as conn_err:\r\n        print(f&quot;Connection error occurred: {conn_err}&quot;)  # 连接错误\r\n    except requests.exceptions.Timeout as timeout_err:\r\n        print(f&quot;Timeout error occurred: {timeout_err}&quot;)  # 超时错误\r\n    except requests.exceptions.RequestException as req_err:\r\n        print(f&quot;An error occurred: {req_err}&quot;)  # 其他请求错误\r\n\r\n    return None\r\n\r\n# 示例调用\r\n# result = describe_image_content(&apos;your_token_here&apos;, &apos;https://example.com/image.jpg&apos;)\r\n# print(result)",
        "Go": "package main\r\n\r\nimport (\r\n\t&quot;encoding/json&quot;\r\n\t&quot;fmt&quot;\r\n\t&quot;io/ioutil&quot;\r\n\t&quot;net/http&quot;\r\n\t&quot;net/url&quot;\r\n)\r\n\r\n// 图片内容描述API的请求参数结构体\r\ntype ImageDescriptionRequest struct {\r\n\tToken string `url:&quot;token&quot;` // 登录获取用户密钥\r\n\tURL   string `url:&quot;url&quot;`   // 需要识别的图片链接\r\n\tType  string `url:&quot;type&quot;`  // 返回数据格式，支持json和text，不填写默认返回text\r\n}\r\n\r\n// 图片内容描述API的响应结构体\r\ntype ImageDescriptionResponse struct {\r\n\tCode    int    `json:&quot;code&quot;`\r\n\tMessage string `json:&quot;message&quot;`\r\n\tData    string `json:&quot;data&quot;`\r\n}\r\n\r\nfunc main() {\r\n\t// 创建请求参数实例，必填参数已填写\r\n\trequest := ImageDescriptionRequest{\r\n\t\tToken: &quot;your_token_here&quot;, // 请替换为实际的token\r\n\t\tURL:   &quot;https://example.com/image.jpg&quot;, // 请替换为实际的图片链接\r\n\t\t// Type:  &quot;json&quot;, // 可选参数，取消注释并设置值以更改返回数据格式\r\n\t}\r\n\r\n\t// 构建请求URL\r\n\trequestURL, err := buildRequestURL(request)\r\n\tif err != nil {\r\n\t\tfmt.Printf(&quot;构建请求URL时出错: %v\\n&quot;, err)\r\n\t\treturn\r\n\t}\r\n\r\n\t// 发送HTTP请求\r\n\tresp, err := sendRequest(requestURL)\r\n\tif err != nil {\r\n\t\tfmt.Printf(&quot;发送HTTP请求时出错: %v\\n&quot;, err)\r\n\t\treturn\r\n\t}\r\n\tdefer resp.Body.Close()\r\n\r\n\t// 读取响应内容\r\n\tbody, err := ioutil.ReadAll(resp.Body)\r\n\tif err != nil {\r\n\t\tfmt.Printf(&quot;读取响应内容时出错: %v\\n&quot;, err)\r\n\t\treturn\r\n\t}\r\n\r\n\t// 根据请求的返回数据格式解析响应\r\n\tif request.Type == &quot;json&quot; {\r\n\t\tvar jsonResponse ImageDescriptionResponse\r\n\t\tif err := json.Unmarshal(body, &amp;jsonResponse); err != nil {\r\n\t\t\tfmt.Printf(&quot;解析JSON响应时出错: %v\\n&quot;, err)\r\n\t\t\treturn\r\n\t\t}\r\n\t\tfmt.Printf(&quot;响应: %+v\\n&quot;, jsonResponse)\r\n\t} else {\r\n\t\tfmt.Printf(&quot;响应: %s\\n&quot;, string(body))\r\n\t}\r\n}\r\n\r\n// 构建请求URL\r\nfunc buildRequestURL(request ImageDescriptionRequest) (string, error) {\r\n\tbaseURL := &quot;https://yunzhiapi.cn/API/tpnrms.php&quot;\r\n\tqueryParams, err := url.ParseQuery(&quot;&quot;)\r\n\tif err != nil {\r\n\t\treturn &quot;&quot;, err\r\n\t}\r\n\r\n\t// 添加必填参数\r\n\tqueryParams.Add(&quot;token&quot;, request.Token)\r\n\tqueryParams.Add(&quot;url&quot;, request.URL)\r\n\r\n\t// 添加可选参数\r\n\tif request.Type != &quot;&quot; {\r\n\t\tqueryParams.Add(&quot;type&quot;, request.Type)\r\n\t}\r\n\r\n\t// 构建完整的请求URL\r\n\trequestURL := baseURL + &quot;?&quot; + queryParams.Encode()\r\n\treturn requestURL, nil\r\n}\r\n\r\n// 发送HTTP请求\r\nfunc sendRequest(requestURL string) (*http.Response, error) {\r\n\tresp, err := http.Get(requestURL)\r\n\tif err != nil {\r\n\t\treturn nil, err\r\n\t}\r\n\r\n\t// 检查HTTP响应状态码\r\n\tif resp.StatusCode != http.StatusOK {\r\n\t\treturn nil, fmt.Errorf(&quot;HTTP请求失败，状态码: %d&quot;, resp.StatusCode)\r\n\t}\r\n\r\n\treturn resp, nil\r\n}",
        "Shell": "#!/bin/bash\r\n\r\n# 设置API请求的URL\r\nAPI_URL=&quot;https://yunzhiapi.cn/API/tpnrms.php&quot;\r\n\r\n# 设置必填参数\r\nTOKEN=&quot;your_token_here&quot;  # 请替换为你的token\r\nURL=&quot;your_image_url_here&quot;  # 请替换为你的图片URL\r\n\r\n# 设置可选参数，取消注释以使用\r\n# TYPE=&quot;json&quot;  # 返回数据格式，支持json和text，不填写默认返回text\r\n\r\n# 检查必填参数是否已设置\r\nif [ -z &quot;$TOKEN&quot; ] || [ -z &quot;$URL&quot; ]; then\r\n  echo &quot;错误: 必填参数未设置。请检查TOKEN和URL变量是否已正确设置。&quot;\r\n  exit 1\r\nfi\r\n\r\n# 构建请求参数\r\nPARAMS=&quot;token=$TOKEN&amp;url=$URL&quot;\r\n\r\n# 如果TYPE参数已设置，则添加到请求参数中\r\nif [ -n &quot;$TYPE&quot; ]; then\r\n  PARAMS=&quot;$PARAMS&amp;type=$TYPE&quot;\r\nfi\r\n\r\n# 发送GET请求\r\nresponse=$(curl -s -G &quot;$API_URL&quot; --data-urlencode &quot;$PARAMS&quot;)\r\n\r\n# 检查响应是否为空\r\nif [ -z &quot;$response&quot; ]; then\r\n  echo &quot;错误: 未收到API响应。请检查网络连接和API URL是否正确。&quot;\r\n  exit 1\r\nfi\r\n\r\n# 输出响应\r\necho &quot;API响应: $response&quot;\r\n#!/bin/bash\r\n\r\n# 设置API请求的URL\r\nAPI_URL=&quot;https://yunzhiapi.cn/API/tpnrms.php&quot;\r\n\r\n# 设置必填参数\r\nTOKEN=&quot;your_token_here&quot;  # 请替换为你的token\r\nURL=&quot;your_image_url_here&quot;  # 请替换为你的图片URL\r\n\r\n# 设置可选参数，取消注释以使用\r\n# TYPE=&quot;json&quot;  # 返回数据格式，支持json和text，不填写默认返回text\r\n\r\n# 检查必填参数是否已设置\r\nif [ -z &quot;$TOKEN&quot; ] || [ -z &quot;$URL&quot; ]; then\r\n  echo &quot;错误: 必填参数未设置。请检查TOKEN和URL变量是否已正确设置。&quot;\r\n  exit 1\r\nfi\r\n\r\n# 构建请求参数\r\nPARAMS=&quot;token=$TOKEN&amp;url=$URL&quot;\r\n\r\n# 如果TYPE参数已设置，则添加到请求参数中\r\nif [ -n &quot;$TYPE&quot; ]; then\r\n  PARAMS=&quot;$PARAMS&amp;type=$TYPE&quot;\r\nfi\r\n\r\n# 发送POST请求\r\nresponse=$(curl -s -X POST &quot;$API_URL&quot; --data-urlencode &quot;$PARAMS&quot;)\r\n\r\n# 检查响应是否为空\r\nif [ -z &quot;$response&quot; ]; then\r\n  echo &quot;错误: 未收到API响应。请检查网络连接和API URL是否正确。&quot;\r\n  exit 1\r\nfi\r\n\r\n# 输出响应\r\necho &quot;API响应: $response&quot;",
        "NodeJS": "const axios = require(&apos;axios&apos;);\r\n\r\n/**\r\n * 图片内容描述API\r\n * @param {string} token - 登录获取用户密钥\r\n * @param {string} url - 需要识别的图片链接\r\n * @returns {Promise} - 返回API响应\r\n */\r\nasync function describeImage(token, url) {\r\n  // 必填参数\r\n  const requiredParams = {\r\n    token: token,\r\n    url: url\r\n  };\r\n\r\n  // 可选参数，用户可根据需要取消注释\r\n  // const optionalParams = {\r\n  //   type: &apos;json&apos; // 返回数据格式，支持json和text，不填写默认返回text\r\n  // };\r\n\r\n  // 构建请求参数\r\n  const params = {\r\n    ...requiredParams\r\n    // ...optionalParams // 如果使用可选参数，取消注释此行\r\n  };\r\n\r\n  try {\r\n    // 发送GET请求\r\n    const response = await axios.get(&apos;https://yunzhiapi.cn/API/tpnrms.php&apos;, { params });\r\n\r\n    // 根据响应格式处理结果\r\n    if (params.type === &apos;json&apos;) {\r\n      return response.data;\r\n    } else {\r\n      return response.data.toString();\r\n    }\r\n  } catch (error) {\r\n    // 处理请求错误\r\n    if (error.response) {\r\n      // 服务器响应错误\r\n      console.error(`服务器响应错误: ${error.response.status} - ${error.response.statusText}`);\r\n      console.error(`响应数据: ${JSON.stringify(error.response.data)}`);\r\n    } else if (error.request) {\r\n      // 请求已发送但没有收到响应\r\n      console.error(&apos;请求已发送但没有收到响应&apos;);\r\n    } else {\r\n      // 一些设置请求时发生错误\r\n      console.error(`请求配置错误: ${error.message}`);\r\n    }\r\n    throw new Error(&apos;API请求失败&apos;);\r\n  }\r\n}\r\n\r\n// 示例调用\r\n// describeImage(&apos;your_token_here&apos;, &apos;https://example.com/image.jpg&apos;)\r\n//   .then(result =&gt; console.log(result))\r\n//   .catch(error =&gt; console.error(error));",
        "Rust": "use reqwest::Client;\r\nuse serde::Deserialize;\r\nuse std::error::Error;\r\n\r\n#[derive(Deserialize, Debug)]\r\nstruct ApiResponse {\r\n    // 根据实际返回的数据结构定义字段\r\n    result: String,\r\n}\r\n\r\n#[tokio::main]\r\nasync fn main() -&gt; Result&lt;(), Box&lt;dyn Error&gt;&gt; {\r\n    // 创建HTTP客户端\r\n    let client = Client::new();\r\n\r\n    // 必填参数\r\n    let token = &quot;your_token_here&quot;; // 登录获取用户密钥\r\n    let url = &quot;https://example.com/image.jpg&quot;; // 需要识别的图片链接\r\n\r\n    // 可选参数，根据需要取消注释并设置值\r\n    // let type_ = &quot;json&quot;; // 返回数据格式，支持json和text，不填写默认返回text\r\n\r\n    // 构建请求URL\r\n    let request_url = &quot;https://yunzhiapi.cn/API/tpnrms.php&quot;;\r\n\r\n    // 构建查询参数\r\n    let mut params = vec![\r\n        (&quot;token&quot;, token),\r\n        (&quot;url&quot;, url),\r\n    ];\r\n\r\n    // 如果使用了可选参数type，取消注释以下行\r\n    // params.push((&quot;type&quot;, type_));\r\n\r\n    // 发送GET请求\r\n    let response = client\r\n        .get(request_url)\r\n        .query(&amp;params)\r\n        .send()\r\n        .await\r\n        .map_err(|e| format!(&quot;请求发送失败: {}&quot;, e))?;\r\n\r\n    // 检查响应状态\r\n    if response.status().is_success() {\r\n        // 根据返回的数据格式解析响应\r\n        // 这里假设返回的是JSON格式，如果是text格式，可以使用response.text().await\r\n        let api_response: ApiResponse = response\r\n            .json()\r\n            .await\r\n            .map_err(|e| format!(&quot;响应解析失败: {}&quot;, e))?;\r\n\r\n        // 打印结果\r\n        println!(&quot;图片内容描述: {}&quot;, api_response.result);\r\n    } else {\r\n        // 打印错误信息\r\n        eprintln!(&quot;请求失败，状态码: {}&quot;, response.status());\r\n        if let Ok(error_body) = response.text().await {\r\n            eprintln!(&quot;错误信息: {}&quot;, error_body);\r\n        }\r\n    }\r\n\r\n    Ok(())\r\n}\r\n请确保在运行此代码之前，替换your_token_here和https://example.com/image.jpg为实际的值。",
        "C++": "#include &lt;iostream&gt;\r\n#include &lt;string&gt;\r\n#include &lt;curl/curl.h&gt;\r\n\r\n// 回调函数，用于处理从服务器返回的数据\r\nsize_t WriteCallback(void* contents, size_t size, size_t nmemb, void* userp) {\r\n    ((std::string*)userp)-&gt;append((char*)contents, size * nmemb);\r\n    return size * nmemb;\r\n}\r\n\r\nint main() {\r\n    // 初始化CURL\r\n    CURL* curl;\r\n    CURLcode res;\r\n    std::string readBuffer;\r\n\r\n    curl = curl_easy_init();\r\n    if(curl) {\r\n        // 必填参数\r\n        std::string token = &quot;YOUR_TOKEN_HERE&quot;; // 请替换为你的token\r\n        std::string url = &quot;YOUR_IMAGE_URL_HERE&quot;; // 请替换为你的图片链接\r\n\r\n        // 可选参数，取消注释以使用\r\n        // std::string type = &quot;json&quot;; // 返回数据格式，支持json和text，默认text\r\n\r\n        // 构建请求URL\r\n        std::string request_url = &quot;https://yunzhiapi.cn/API/tpnrms.php?token=&quot; + token + &quot;&amp;url=&quot; + url;\r\n        // 如果使用可选参数type，取消注释以下行\r\n        // request_url += &quot;&amp;type=&quot; + type;\r\n\r\n        // 设置CURL选项\r\n        curl_easy_setopt(curl, CURLOPT_URL, request_url.c_str());\r\n        curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, WriteCallback);\r\n        curl_easy_setopt(curl, CURLOPT_WRITEDATA, &amp;readBuffer);\r\n\r\n        // 执行请求\r\n        res = curl_easy_perform(curl);\r\n\r\n        // 检查请求是否成功\r\n        if(res != CURLE_OK) {\r\n            std::cerr &lt;&lt; &quot;请求失败: &quot; &lt;&lt; curl_easy_strerror(res) &lt;&lt; std::endl;\r\n        } else {\r\n            std::cout &lt;&lt; &quot;服务器响应: &quot; &lt;&lt; readBuffer &lt;&lt; std::endl;\r\n        }\r\n\r\n        // 清理CURL\r\n        curl_easy_cleanup(curl);\r\n    } else {\r\n        std::cerr &lt;&lt; &quot;无法初始化CURL&quot; &lt;&lt; std::endl;\r\n    }\r\n\r\n    return 0;\r\n}",
        "C": "#include &lt;stdio.h&gt;\r\n#include &lt;stdlib.h&gt;\r\n#include &lt;string.h&gt;\r\n#include &lt;curl/curl.h&gt;\r\n\r\n// 回调函数，用于处理HTTP响应\r\nsize_t WriteCallback(void *contents, size_t size, size_t nmemb, void *userp) {\r\n    size_t totalSize = size * nmemb;\r\n    printf(&quot;Response: %s\\n&quot;, (char*)contents);\r\n    return totalSize;\r\n}\r\n\r\nint main() {\r\n    CURL *curl;\r\n    CURLcode res;\r\n\r\n    // 初始化libcurl\r\n    curl = curl_easy_init();\r\n    if(curl) {\r\n        // 必填参数\r\n        const char *token = &quot;your_token_here&quot;; // 登录获取用户密钥\r\n        const char *url = &quot;https://example.com/your_image.jpg&quot;; // 需要识别的图片链接\r\n\r\n        // 可选参数\r\n        // const char *type = &quot;json&quot;; // 返回数据格式，支持json和text，不填写默认返回text\r\n\r\n        // 构造请求URL\r\n        char request_url[1024];\r\n        snprintf(request_url, sizeof(request_url), &quot;https://yunzhiapi.cn/API/tpnrms.php?token=%s&amp;url=%s&quot;, token, url);\r\n\r\n        // 如果需要使用可选参数，取消注释下面的代码\r\n        // snprintf(request_url, sizeof(request_url), &quot;https://yunzhiapi.cn/API/tpnrms.php?token=%s&amp;url=%s&amp;type=%s&quot;, token, url, type);\r\n\r\n        // 设置请求URL\r\n        curl_easy_setopt(curl, CURLOPT_URL, request_url);\r\n\r\n        // 设置回调函数以处理响应数据\r\n        curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, WriteCallback);\r\n\r\n        // 执行请求\r\n        res = curl_easy_perform(curl);\r\n\r\n        // 检查请求是否成功\r\n        if(res != CURLE_OK) {\r\n            fprintf(stderr, &quot;curl_easy_perform() failed: %s\\n&quot;, curl_easy_strerror(res));\r\n        }\r\n\r\n        // 清理libcurl\r\n        curl_easy_cleanup(curl);\r\n    } else {\r\n        fprintf(stderr, &quot;Failed to initialize curl\\n&quot;);\r\n    }\r\n\r\n    return 0;\r\n}\r\n请确保在编译和运行此代码之前安装了libcurl库，并且正确设置了编译选项以链接libcurl。例如，在Linux上可以使用以下命令编译：\r\n\r\ngcc -o image_description image_description.c -lcurl"
    }
}